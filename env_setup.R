# the tidyverse is a dynamic collection of many essential R packages
install.packages("tidyverse")

# this tutorial also uses these
install.packages(c("nycflights13", "gapminder", "Lahman"))

# to update packages run tidyverse_update()

